# GFVP Compiled Prompt QC - P007 Relationship v0.2

Compiled Prompt: GFVP-COMPILED-P007-RELATIONSHIP_PROMPT_v0-2
QC Status: Pass / Ready for Label-Hygiene Regeneration
Date: 2026-07-03

## QC Result

The compiled P007 Relationship v0.2 prompt passes compiled prompt QC.

It is ready for label-hygiene regeneration.

## Revision Scope

Pass.

The prompt preserves the v0.1 structure and does not request a redesign.

The regeneration target is narrow: exact spelling of metadata labels, especially `Status`.

## Label Hygiene

Pass.

The prompt explicitly requires:

`Status: Sandbox / Candidate`

It also forbids misspelled variants including `Ststus`.

## Object Fidelity

Pass.

The prompt still compiles one object: Relationship.

It preserves the Link / Connector Plate structure with one central Relationship object between `FROM OBJECT` and `TO OBJECT`.

## Canon Boundary

Pass.

The prompt blocks canonization, validation, promotion, final-truth language, and production-process language.

## Decision

Open the image gate for P007 v0.2 label-hygiene regeneration only.

